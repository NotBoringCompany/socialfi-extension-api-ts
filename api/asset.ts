import { RestorationItem } from '../models/item';
import { ReturnValue } from '../utils/retVal';

// export const consumeRestorationItem = async (twitterId: string, item: RestorationItem): Promise<ReturnValue> => {

// }